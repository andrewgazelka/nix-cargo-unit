[package]
name = "example-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core library with build script for nix-cargo-unit testing"

[build-dependencies]
# No external deps - pure build script
